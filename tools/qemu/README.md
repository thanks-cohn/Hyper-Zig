# QEMU Tooling

The first hardware target is the QEMU `virt` RISC-V machine. Tooling in this
directory should capture pinned QEMU versions, launch notes, and debug recipes
as V0 implementation begins.
